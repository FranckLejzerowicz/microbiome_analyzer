{
    "main": "The `filter` folder contains jobs for the filtering of features in the samples based on their prevalence and abundances, as well of samples based on their names (as specified in the filtering configuration file passed to the `-f` option of `microbiome_analyzer`). It can be useful to explore the results in `filter3d` to find a filtering thresholds. Note that filtering is done in this order: (1) remove samples listed by names, (2) keep only the samples having at least the given number of reads, and (3) remove features having less that the number/percent of reads in these samples (for each sample individually).\n\nIt is done directly within microbiome_analyzer.",
    "dataset": "the filtering folders are in `data`, and for each dataset therein, are named after the applied filters, such as <#rm-sam###-feat###>, with:\n- <#rm> indicating the number of removed samples;\n- <sam###> indicating the min number of reads in the samples;\- <feat###> indicating the min number (if number does not start with '0') or the min fraction (add a '.' after the first '0').\nFor example:\n- a folder named `sam10000-feat0001` is for a feature table with samples having ≥10,000 reads and in each samples, the features having less than 0.1 % (0.001 as fraction) of the total reads in the sample were removed";\n- a folder named `10rm-sam10000-feat0001` underwent the same filters, except for 10 samples that were first removed",
    "output": "The filtered feature tables are in the same format as for the unfiltered outputs of the `data` folders:\n- `table.tsv`: filtered feature table (as tab-separated format: rows=features; columns=samples)\n- `table.biom`: filtered feature table in BIOM format\n- `table.qza`: filtered feature table (as QIIME2 Artefact)\n- `table.qzv`: QIIME2 Interactive Visualization containing summaries of the samples and features (load in https://view.qiime2.org)",     "version": "Generated from configuration file\nCONFIGFILE\nCONFIGMD5",
}

