{
    "main": "The `taxonomy` folder contains the jobs and outputs to import the taxonomy tables prepared here in `.tsv` format into the QIIME2 `.qza` format for use in analyses. This is one of the first step since `microbiome_analyzer` needs a `taxonomy.tsv` file placed in a subfolder that inside the `taxonomy` folder, must be named after the name of the dataset",
    "dataset": "the first folders in `taxonomy` are each of the datasets.",
    "qiime2 command": "`qiime tools import`",
    "qiime2 reference": "https://amplicon-docs.qiime2.org/en/stable/explanations/experienced-researchers.html#importing-data-into-qiime-2-qiime-tools-import",
    "output": "Importing into QIIME2 consists of creating the first `taxonomy.qza` file (called an "Artefact") for the mapping from feature to taxon, which is then used for all configured analyses:\n- `taxonomy.qza`: two columns "Feature ID" and "Taxon" that maps feature to taxonomy (in QIIME2 Artefact format)\n- `taxonomy.tsv`: two columns "Feature ID" and "Taxon" that maps feature to taxonomy (in tab-separated format)\n- `taxonomy_filtempty<rank>.tsv`: lists which features have no taxonomy as a given rank (for filtering of these features and avoid collapsing all unassigned features under an intermediate taxon\n- `taxonomy_splitaxa.txt`: feature to taxon mapping with the taxa separated as columns for each taxonomic level",    "version": "Generated from configuration file\nCONFIGFILE\nCONFIGMD5"
}