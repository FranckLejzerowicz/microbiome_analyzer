{
    "main": "The `collapse` folder contains jobs and their outputs which consists of feature tables with read counts collapsed to new features corresponding to taxa at those taxonomic levels specified for each dataset in the configuration file passed to `microbiome_analyzer` via the `-c` option.\n\nIt is done in QIIME2 using the q2-diversity library",
    "qiime2 command": "`qiime taxa collapse`",
    "qiime2 reference": "https://amplicon-docs.qiime2.org/en/stable/references/plugins/taxa.html#q2-action-taxa-collapse",
    "dataset": "the first folders in `collapse ` are each of the datasets.",
    "output": "The collapsed feature tables are placed in folder named `tx-<taxonomic_level>` and then used for all configured analyses. They are automatically used an input to make summaries:\n- `table.tsv`: collapsed feature table prepared by the user (rows=features; columns=samples)\n- `table.biom`: collapsed feature table can be in BIOM format\n- `table.qza`: collapsed feature table after as QIIME2 Artefact\n- `table.qzv`: QIIME2 Interactive Visualization containing summaries of the samples and taxa (load in https://view.qiime2.org)",     "version": "Generated from configuration file\nCONFIGFILE\nCONFIGMD5",
}
