{
    "main": "The `mmvec` folder contains jobs and their outputs to infer microbe-metabolite interactions by learning latent vector representations from paired omics datasets (typically microbiomes and metabolomes). It models the conditional probability of observing a metabolite given the presence of a microbe, using neural networks to estimate co-occurrence patterns.\n\nIt is done in QIIME2 using the q2-mmvec library\nAttention: The mmvec QIIME2 plugin is only compatible with QIIME2 environments up to version 2020.6. It is recommended to the create a "qiime2-2020.6-mmvec" conda environment and have it used by specifying it as value to the `env` key in the user parameters file (option `-u` in `microbiome_analyzer run`).",
    "reference": "Morton, J.T., Aksenov, A.A., Nothias, L.F., Foulds, J.R., Quinn, R.A., Badri, M.H., Swenson, T.L., Van Goethem, M.W., Northen, T.R., Vazquez-Baeza, Y. and Wang, M., 2019. Learning representations of microbe–metabolite interactions. Nature methods, 16(12), pp.1306-1314.",
    "doi": "https://doi.org/10.1038/s41592-019-0616-3",
    "qiime2 command": "`qiime mmvec paired-omics`",
    "github": "https://github.com/biocore/mmvec"
    "dataset": "the first folders in `mmvec` are:\n- `paired`: contains one folder per pair of datasets, as specified in the `mmvec` configuration file.\n- `datasets`: feature tables for each dataset per pair.\n- `metadata`: sample metadata used for the filtering and notably to identify the samples in common.",
    "output": "For each paired datasets and after running `mmvec` for a given subset, filters, hyperparameters, the outputs consist of:\n
- `model` folder, containing:\n  - `ordination_emperor.qzv`: QIIME2 Interactive Visualization of the dimensionality reduction on the conditional probablities (with few features as biplot)\n  - `ordination_emperor_crowded.qzv`: QIIME2 Interactive Visualization of the dimensionality reduction on the conditional probablities (with all features as biplot)\n  - `ordination.qza`: dimensionality reduction on the conditional probablities (as QIIME2 Artefact)\n  - `ordination.txt`: dimensionality reduction on the conditional probablities (as tab-separated format)\n  - `ranks.html`: interactive heatmap to visualize the main co-occurrences and their feature metadata (generated using https://github.com/FranckLejzerowicz/xmmvec). It is necessary to setup the metadata for features (see `feature_metadata` folder) and to refer to the metadata variable to show in the configuration file (see `mmbird`, as it automatically integrates the differentials of `songbird` into `mmvec` plots.\n  - `ranks.qza`: conditional probabilities, a matrix for pairwise matrix for each feature in the first vs features of the other omics dataset (as QIIME2 Artefact)\n  - `ranks.tsv`: conditional probabilities, a matrix for pairwise matrix for each feature in the first vs features of the other omics dataset (as tab-separated format)\n  - `stats.qza`: statistics of the `mmvec` learning process for the multi-omics model (QIIME2 Artefact)\n- `null` folder, containing:\n  - `ordination.qza`: dimensionality reduction on the conditional probablities for the random model used as baseline (as QIIME2 Artefact)\n  - `ranks.qza`: conditional probabilities for the ranbdom model used as baseline (as QIIME2 Artefact)\n  - `stats.qza`: statistics of the `mmvec` learning process for a random model, to serve as baseline vs the multi-omics model (QIIME2 Artefact)\n- `paired-summary.qzv`: QIIME2 Visualization of the diagnostic comparing the multi-omics models vs baseline model. The cross-validation error and log-likelihood curves should level down, and the test model has learned differentials vs the baseline is its error is lower that for the baseline (also captured in the Pseudo-Q^2 score, at the end of the iterations). See paper/github for more explanations;\n- `paired-summary.html `: HTML export of the QIIME2 Visualization",
     "version": "Generated from configuration file"
}