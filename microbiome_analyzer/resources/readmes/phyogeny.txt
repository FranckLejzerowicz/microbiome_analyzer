{
    "main": "The `phylogeny ` folder contains the jobs for the importing of the phylogenetic trees located in the `phylogeny` folder for use in QIIME2, i.e., the prepared phylogenetic trees (with features of the feature in `data` as tips) named `tree.nwk`, an (optional) requisite for `microbiome_analyzer` to use phylogenetic metrics." This is a required step for phylogenetic analyses since `microbiome_analyzer` matches any existing `.nwk` file placed in a subfolder that inside the `data` folder must be named after the name of the dataset. A dataset without a tree will be considered as having not a phylogenetic context for analysis",
    "dataset": "the first folders in `phyolgeny ` are each of the datasets.",
    "output": "Importing into QIIME2 consists of creating the first `tree.qza` file (called an Artefact) for the dataset, which is then used for all configured analyses. It is automatically used to make an interactive QIIME2 Visualization `tree.qzv` that can be explored (load in https://view.qiime2.org)",
    "version": "Generated from configuration file"
}