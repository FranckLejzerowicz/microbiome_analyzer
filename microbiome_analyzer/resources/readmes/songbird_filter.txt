{
    "main": "The `songbird` folder contains inputs and outputs to estimate differentially abundant features in a microbiome dataset. As explained in the method's code documentation, it measures log-fold changes for microbes, metabolites, functions (etc) for models built based on sample metadata information. These differentials are ranks that indicate the relative association of features with the model covariates.\nSONGBIRD relies on a single-layer neural network with hyperparamteres such as learning rate, batch size and epoch to learn these differential from the dataset. A good rule of thumb is that feature used for modelling should appear at least in 10 samples (to draw lines) but then, check the diagnostic outputs to spot overfitting or too little learning, i.e., how the data is used on train and testing.",
    "reference": "Morton, J.T., Marotz, C., Washburne, A. et al. Establishing microbial composition measurement standards with reference frames. Nat Commun 10, 2719 (2019).",
    "github": "https://github.com/biocore/songbird",
    "doi": "https://doi.org/10.1038/s41467-019-10656-5",
    "multi-omics": "`songbird` models yield differentials that can be used as metadata, but for the features. Thus, the differentials can be represented on any multi-omics/network analysis showing these features. Yet, multi-omics tools (such as `mmvec`) may have been run on the subset of samples that exist in both omics datasets ("paired" samples), i.e. on the features that occur in these paired samples. Hence, the differentials for a full omics dataset and for the subset corresponding to the samples also present in another omics dataset, are obtained using separate runs of `songbird`.",
    "dataset": "For each dataset, there is always a subfolder named `unpaired` which contains the modelling results on the full dataset, whereas other folders may be present alongside and named after the paired datasets (as defined in multi-omics configuration files, e.g.,  see `mmvec`).",
    "outputs": "Within a series of subfolders to sort the different filters, subsets, and hyperparameters, and models (named in the `songbirg` configuration file), there is always:\n- `b-<baseline_model>`: subfolder containing the modelling results for a model serving as baseline for the tested model (if this model was already run as a test model, only its *stats* file is used, to compute the learning gain, in the diagnostic plot):\n- `differentials-baseline.qza`: differential for the baseline model;\n- `differentials-biplot-baseline.qza`: singular-value decomposition biplot of the model terms and features for the baseline model;\n- `differentials-stats-baseline.qza`: learning stats for the baseline model;\n- `tensorboard.qzv`: QIIME2 Visualization of the diagnostic comparing the test vs baseline models. The cross-validation error and log-likelihood curves should level down, and the test model has learned differentials vs the baseline is its error is lower that for the baseline (also captured in the Pseudo-Q^2 score, at the end of the iterations). See paper/github for more explanations;\n- `tensorboard.html`: HTML export of the QIIME2 Visualization;\n- `differentials.qza`: differential for the test model (as QIIME2 Artefact file);\n- `differentials.tsv`: differential for the test model (in tab-separated format);\n- `differentials_qurro.qzv`: interactive figure to explore log-ratios formed by features that can be selected based on their differentials (generated by `qurro`);\n- `differentials-biplot.qza`: singular-value decomposition biplot of the model terms and features for the test model;\n- `differentials-stats.qza`: learning stats for the test model (as QIIME2 Artefact file);\n- `differentials-stats.txt (in tab-separated format)`;\n- `metadata.tsv`: sample metadata with the variable used in the test and baseline models;\n- `tab.qza`: feature table for the current subset and after the current filters\n- `readme.txt`: recapitulation of the parameters for this run of `songbird`.",
    "version": "Generated from configuration file\nCONFIGFILE\nCONFIGMD5"
}
