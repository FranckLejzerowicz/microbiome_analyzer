{
    "main": "The `sourcetracking` folder contains jobs and outputs to perform microbial source tracking which aims to quantify the proportion of different microbial composition (the "sources") contributing to a target microbial community (the "sink" under scrunity). This is done using Xsourcetracking, which used comnand line to run SourceTracker2 and FEAST (and he QIIME2, Random Forest-based sample classifier).\n- SourceTracker2 uses a Bayesian framework with Markov chain Monte Carlo sampling to estimate the contribution of each potential source environment (e.g., sediment, soil) to a sink sample (e.g., air). It is well-suited for metagenomics studies but can be computationally intensive, especially with large datasets.\n- FEAST (Fast Expectation-Maximization for Microbial Source Tracking) is a faster, more scalable alternative that uses expectation-maximization algorithms to estimate source contributions. It improves computational efficiency while maintaining accuracy, making it ideal for analyzing large-scale microbiome data with thousands of potential source environments.\nBoth tools rely on species abundance profiles from known source samples and a target sink sample to infer source proportions. However, they tend to overestimate known source contributions and underestimate unknown or unmeasured sources, which can limit accuracy in complex environments.\n\nIt is run via Xsourcetracking, which prepares and runs commands for Sourcetracker2 and FEAST, based on the configuration file passed to `microbiome_analzyer` using option `-st`, to specify the source-sink pairs based on metadata variables, and the tools to use.",
    "reference": "Shenhav, L., Thompson, M., Joseph, T.A. et al. FEAST: fast expectation-maximization for microbial source tracking. Nat Methods 16, 627–632 (2019).\nKnights, D., Kuczynski, J., Charlson, E.S., Zaneveld, J., Mozer, M.C., Collman, R.G., Bushman, F.D., Knight, R. and Kelley, S.T., 2011. Bayesian community-wide culture-independent microbial source tracking. Nature methods, 8(9), pp.761-763.",
    "github": "https://github.com/FranckLejzerowicz/Xsourcetracking\nhttps://github.com/cozygene/FEAST\nhttps://github.com/caporaso-lab/sourcetracker2",
    "doi": "https://doi.org/10.1038/s41592-019-0431-x\nhttps://doi.org/10.1038/nmeth.1650",
    "dataset": "the first folders in `sourcetracking` are each of the datasets. Then, inside subfolders for each samples subset, there is a folder per sink (named after a metadata variable) and a folder per tool (`feast`, `sourcetracker2`, or `q2-classifier`).",
    "outputs": "For the `feast` subfolder, the outputs are the same in each iteration represented by a folder `t#`:\n- `map.r0.tsv`: mapping file for the sources and sinks (column `SourceSink`), for each sample (`SampleID`), whichin `Env` lists unique sink name per sink sample,  while all source samples can hav the same `Env` (the sinks must also have a numeric `id`)\n- `map.r1.tsv`: Same as `map.r0.tsv`, but with a different random draw of samples used a sink for the given factor (so that sources may not miss samples that would only be used as sinks for the current variable's factor to test as a source too)\n- `out.r0_source_contributions_matrix.txt`: proportions that the source microbial composition contribute to a target sink (for the first draw)\n- `out.r1_source_contributions_matrix.txt`: proportions that the source microbial composition contribute to a target sink (for the second draw)\For the `sourcetracker2` subfolder: README TO BE COMPLETED",
    "version": "Generated from configuration file\nCONFIGFILE\nCONFIGMD5"
}
